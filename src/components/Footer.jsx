import React from "react";

const Footer = ({
  name,
  email,
  phone,
  bankName,
  bankHolderName,
  bankAccNumber,
  website,
}) => {
  return <footer className="footer border-t-2 border-gray-300 pt-5"></footer>;
};

export default Footer;
